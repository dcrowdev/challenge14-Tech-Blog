<div class="container-fluid">
    {{#each posts as |post|}}
    <div class="card-header">
        {{post_title}}
    </div>
    <div class="card-body">
        {{post_body}}
    </div>
    {{/each}}
</div>